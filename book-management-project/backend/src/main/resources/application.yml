openai:
  api:
    key: 여기에 OpenAI API key 입력

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/book
    username: sa
    password: 1234
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true